describe('Go to Twitter and Login', () => {
    it('Homepage', () => {
        cy.visit('https://www.ebay.com/');

    });

    it('search for stuff', () => {

        cy.findByText('').click();
        cy.get('').click();
    });




});